# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Environment

- Python 3.11 via `.venv/` (created by PyCharm)
- Activate: `source .venv/bin/activate`
- Run scripts: `python main.py`

## Project state

This is a new, empty PyCharm project. Only `main.py` exists with a placeholder `print_hi` function. No dependencies, tests, or build tooling have been set up yet.
